#!/bin/bash
# setup.sh
# Turns a stock Ubuntu 16 install into a gallery host

# Install prerequisite software

# Create gallery user to run everything

# Setup virtualenv for /var/www/gallery

# make sure gallery:www-data owns /var/www/gallery

# Configure uWSGI in systemd

# Create systemd service so uWSGI starts constantly

# Configure nginx to point to uWSGI
